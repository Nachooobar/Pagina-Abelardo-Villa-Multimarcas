<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Login - Abelardo Villa Multimarcas</title>
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
  <div class="login-page">
    <div class="login-card">
      <img src="/images/logo.png" alt="Abelardo Villa Multimarcas" style="width:100%;max-width:280px;margin:0 auto 1.5rem;display:block;border-radius:8px;">
      <h1>🔐 Panel de Administración</h1>
      <p class="login-sub">Ingresá tus credenciales para acceder</p>
      <% if (error) { %>
        <div class="alert alert-danger">⚠️ <%= error %></div>
      <% } %>
      <form method="POST" action="/admin/login">
        <div class="form-group">
          <label for="username">Usuario</label>
          <input type="text" id="username" name="username" required placeholder="Tu usuario" autocomplete="username">
        </div>
        <div class="form-group">
          <label for="password">Contraseña</label>
          <input type="password" id="password" name="password" required placeholder="••••••••" autocomplete="current-password">
        </div>
        <button type="submit" class="btn btn-primary btn-block" style="margin-top:0.75rem;padding:0.9rem;font-size:1rem;">
          Ingresar →
        </button>
      </form>
      <p style="text-align:center; margin-top:1.5rem;">
        <a href="/" style="color:var(--admin-text-muted); font-size:0.85rem; text-decoration:none;">← Volver al sitio</a>
      </p>
    </div>
    <div style="position:absolute;bottom:1.25rem;left:0;right:0;display:flex;align-items:center;justify-content:center;gap:0.4rem;opacity:0.35;">
      <span style="font-size:0.65rem;color:rgba(255,255,255,0.6);letter-spacing:0.3px;">Desarrollado por</span>
      <img src="/images/astroweb-logo.png" alt="Astro Web" style="height:20px;width:auto;object-fit:contain;filter:brightness(0) invert(1);opacity:0.7;">
    </div>
  </div>
</body>
</html>
